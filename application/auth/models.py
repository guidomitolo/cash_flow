from application import db
from flask_login import UserMixin
from werkzeug.security import generate_password_hash, check_password_hash


class User(db.Model, UserMixin):
    __tablename__ = 'user'

    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(64), index=True, unique=True)
    email = db.Column(db.String(120), index=True, unique=True)
    password_hash = db.Column(db.String(128))

    balance = db.relationship('Balance', backref='user_balance', lazy='dynamic')
    credit = db.relationship('Credit', backref='user_credit', lazy='dynamic')
    credit_card = db.relationship('CreditCard', backref='user_credit_card', lazy='dynamic')
    credit_statement = db.relationship('CreditStatement', backref='user_credit_statement', lazy='dynamic')

    def __repr__(self):
        return self.username

    def set_password(self, password):
        self.password_hash = generate_password_hash(password)

    def check_password(self, password):
        return check_password_hash(self.password_hash, password)

    def __repr__(self):
        return '<User {}>'.format(self.username)
